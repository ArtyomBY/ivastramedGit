import React from 'react';
import { Typography } from '@mui/material';

const PatientList: React.FC = () => {
  return (
    <div>
      <Typography variant="h6">Список пациентов</Typography>
      {/* Здесь будет функционал списка пациентов */}
    </div>
  );
};

export default PatientList;
