import React from 'react';
import { Typography } from '@mui/material';

const Notifications: React.FC = () => {
  return (
    <div>
      <Typography variant="h6">Уведомления</Typography>
      {/* Здесь будет функционал уведомлений */}
    </div>
  );
};

export default Notifications;
